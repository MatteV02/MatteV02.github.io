---
// Assets
import Logo from "@/components/assets/Logo.astro";
// Fundations
import Text from "@/components/fundations/elements/Text.astro";
import Button from "@/components/fundations/elements/Button.astro";
import Wrapper from "@/components/fundations/containers/Wrapper.astro";
import { getEntry } from "astro:content";

const topBarEntry = await getEntry("globals", "top_bar");
const { location = "", role = "", links = [], action } = topBarEntry?.data || {};
---

<nav class="bg-white dark:bg-base-950 fixed top-0 z-10 w-full">
  <Wrapper variant="standard" class="py-2">
    <div class="grid grid-cols-3 lg:grid-cols-4 items-start">
      <a href="/" class="-mt-1 flex">
        <Logo
          class="text-base-600 dark:text-base-300 text-xs tracking-tight font-base-900"
        />
      </a>
      <Text
        tag="p"
        variant="textXS"
        class="text-base-600 dark:text-base-300 tracking-tight font-sans"
      >
        {location}<br />{role}
      </Text>
      <div class="hidden lg:flex gap-8 text-sm items-center">
        {
          links?.map((link) => (
            <a
              href={link.href}
              class="text-base-600 dark:text-base-300  tracking-tight text-xs"
            >
              {link.text}
            </a>
          ))
        }
      </div>
      <a href={action?.href}
        class="text-base-600 dark:text-base-300 text-xs tracking-tight font-sans ml-auto"
        >{action?.text}
      </a>
    </div>
  </Wrapper>
</nav>
